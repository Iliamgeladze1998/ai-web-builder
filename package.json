const express = require('express');
const path = require('path');
const app = express();

// მთავარი გვერდის გაშვება
app.get('/', (req, res) => {
    res.sendFile(path.join(__dirname, 'index.html'));
});

// პორტი Koyeb-ისთვის
const PORT = process.env.PORT || 3000;
app.listen(PORT, () => {
    console.log(`სერვერი ჩაირთო პორტზე: ${PORT}`);
});

